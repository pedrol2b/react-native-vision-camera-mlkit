buildscript {
  ext {
    def v2FeaturesEnabled =
      !Boolean.parseBoolean(System.getenv("VISION_CAMERA_MLKIT_DISABLE_V2_FEATURES"))

    buildToolsVersion = "36.0.0"
    minSdkVersion = 24
    compileSdkVersion = 36
    targetSdkVersion = 36
    ndkVersion = "27.1.12297006"
    kotlinVersion = "2.1.20"

    set('react-native-vision-camera-mlkit', [
      mlkit: [
        textRecognition          : v2FeaturesEnabled,
        textRecognitionChinese   : v2FeaturesEnabled,
        textRecognitionDevanagari: v2FeaturesEnabled,
        textRecognitionJapanese  : v2FeaturesEnabled,
        textRecognitionKorean    : v2FeaturesEnabled,
        faceDetection            : false,
        faceMeshDetection        : false,
        poseDetection            : false,
        poseDetectionAccurate    : false,
        selfieSegmentation       : false,
        subjectSegmentation      : false,
        documentScanner          : false,
        barcodeScanning          : v2FeaturesEnabled,
        imageLabeling            : false,
        objectDetection          : false,
        digitalInkRecognition    : false,
      ]
    ])
  }
  repositories {
    google()
    mavenCentral()
  }
  dependencies {
    classpath("com.android.tools.build:gradle")
    classpath("com.facebook.react:react-native-gradle-plugin")
    classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
  }
}

apply plugin: "com.facebook.react.rootproject"
